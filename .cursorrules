# Cursor AI 规则文件

## 项目概述
项目名称: drink-reminder
项目类型: 桌面应用程序
技术栈: Python 3.x + tkinter
架构模式: MVC (Model-View-Controller)

## 代码风格规范
1. 遵循 PEP 8 编码规范
2. 使用类型注解（Type Hints）
3. 函数和类必须有文档字符串（Docstrings）
4. 变量命名清晰易懂，使用有意义的名称
5. 避免魔法数字，使用常量或配置项
6. 最大行长度: 88 字符（Black 格式）
7. 导入顺序: 标准库 -> 第三方库 -> 本地模块

## 架构设计规范
1. **Model 层**（models/）:
   - 负责数据管理和业务逻辑
   - 不包含任何 UI 相关代码
   - 提供清晰的数据操作接口

2. **View 层**（views/）:
   - 负责 GUI 界面显示
   - 处理用户交互事件
   - 通过回调与 Controller 通信

3. **Controller 层**（controllers/）:
   - 协调 Model 和 View
   - 处理业务流程
   - 实现主循环逻辑

4. **Utils 层**（utils/）:
   - 提供通用工具函数
   - 日志记录、文件操作等

5. **Config 层**（config.py）:
   - 集中管理所有配置项
   - 便于配置修改和维护

## Git 提交规范
使用 Conventional Commits 格式:
- 格式: `<type>(<scope>): <subject>`
- 类型（type）:
  - `feat`: 新功能
  - `fix`: 修复 bug
  - `docs`: 文档变更
  - `style`: 代码格式调整
  - `refactor`: 代码重构
  - `test`: 测试相关
  - `chore`: 构建/工具/依赖更新
  - `perf`: 性能优化
  - `ci`: CI/CD 配置
  - `build`: 构建系统
  - `revert`: 回滚提交

- 范围（scope）:
  - `models`: 数据模型层
  - `views`: 视图层
  - `controllers`: 控制器层
  - `utils`: 工具层
  - `config`: 配置管理
  - `tests`: 测试文件
  - `docs`: 文档

- 示例:
  - `feat(controllers): 添加新的提醒控制器`
  - `fix(models): 修复数据加载时的日期解析错误`
  - `docs(readme): 更新安装说明`
  - `refactor: 优化时间判断逻辑`

## 测试规范
1. 每个功能模块都需要对应的测试文件
2. 测试文件命名: `test_*.py`
3. 使用 `unittest` 或 `pytest` 框架
4. 测试覆盖率目标: >80%
5. 测试应该独立运行，不依赖外部状态

## 文档规范
1. 所有公共函数和类必须有文档字符串
2. 使用 Google 风格或 NumPy 风格的文档字符串
3. 复杂逻辑需要添加行内注释
4. 保持 README.md 和 ARCHITECTURE.md 更新

## 性能考虑
1. 避免在主循环中进行耗时操作
2. 使用异步或线程处理长时间任务
3. 合理使用缓存
4. 优化数据加载和保存

## 安全考虑
1. 用户数据文件（drink_data.json）不提交到 Git
2. 敏感信息不硬编码
3. 输入验证和错误处理
4. 文件操作使用异常处理

## 版本管理
1. 遵循语义化版本（Semantic Versioning）
2. 格式: MAJOR.MINOR.PATCH
3. MAJOR: 不兼容的 API 变更
4. MINOR: 向后兼容的功能新增
5. PATCH: 向后兼容的问题修复

## 开发工作流
1. 从 main 分支创建功能分支
2. 完成开发和测试
3. 提交代码（遵循提交规范）
4. 合并到 main 分支
5. 删除功能分支

## 优先级
1. 代码可读性和可维护性
2. 测试覆盖率
3. 性能优化
4. 功能完整性
